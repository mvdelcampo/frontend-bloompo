import {
    Platform,
    SafeAreaView,
    StyleSheet,
    View,
    Text,
    ScrollView,
    TouchableOpacity,
    FlatList,
} from "react-native";

export const options = {
    headerShown: false,
    title: "" 
};

export default function EditGroupScreen() {
    return (
        <Text> hola editar grupo</Text>
    )
}
